{
  "name": "taskmanagerplus-ai-analyzer",
  "version": "0.1.0",
  "private": true,
  "description": "Analyzer for automated test reports with optional AI step.",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:tests": "tsc -p tsconfig.vitest.json --noEmit",
    "dev": "tsx src/main.ts sample-report.json",
    "start": "node dist/main.js sample-report.json",
    "test": "vitest --run -c vitest.config.ts",
    "test:watch": "vitest -c vitest.config.ts",
    "test:ai": "vitest --run tests/ai/aiAnalyzer.test.ts",
    "analyze:real": "node dist/main.js \"C:\\\\Desenvolvimento\\\\workspace\\\\taskmanagerplus-tests\\\\ui-tests\\\\reports\\\\ui\\\\playwright-report.json\"",
    "analyze:real:free": "cross-env AI_PROVIDER=off AI_ENABLED=false AI_BUDGET_CENTS=0 npm run analyze:real",
    "analyze:real:mock": "cross-env AI_PROVIDER=mock AI_ENABLED=true AI_BUDGET_CENTS=0 npm run analyze:real",
    "analyze:real:openai": "cross-env AI_PROVIDER=openai AI_ENABLED=true AI_BUDGET_CENTS=500 npm run analyze:real"
  },
  "devDependencies": {
    "@types/node": "^22.18.10",
    "cross-env": "^10.1.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.6",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "openai": "^4.104.0"
  }
}
